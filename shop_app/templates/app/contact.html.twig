{% extends 'base.html.twig' %}

{% block body %}

<div class="jumbotron text-center">
    <h1>Contact</h1>
</div>

{{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Envoyer</button>
    {{ form_end(form) }}
    
{% endblock %}